---
import Tag from "@/components/Tag.astro"

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<Tag color="amber" icon="icon-[lucide--calendar-clock]" class={className}>Draft</Tag>
