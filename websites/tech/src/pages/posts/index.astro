---
import PageTitle from "@zlliang/theme/components/PageTitle.astro"
import Layout from "@/components/Layout.astro"
import PostList from "@/components/PostList.astro"

import { getPosts } from "@/utils/content"

const posts = await getPosts()
const title = "Posts"
---

<Layout title={title} showRecentPosts={false}>
  <!-- Title -->
  <PageTitle>
    <Fragment>{title}</Fragment>
    <span slot="subtitle">{posts.length} total</span>
  </PageTitle>

  <!-- Posts -->
  {posts.length > 0 ? <PostList posts={posts} /> : <p class="text-neutral-500">No posts found.</p>}
</Layout>
